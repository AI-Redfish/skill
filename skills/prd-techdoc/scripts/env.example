# prd-techdoc 本地 Python 脚本环境信息示例
# 复制到 skill 运行时工作目录的 .agents\.env（UTF-8），按需填写；全部为可选项。
# 脚本依赖用 uv 管理（uv run 自动隔离安装）；本文件只被脚本读取，不被写入。
# 需要补充时由主调用方提示用户输入后写入。
# 注意：本文件可能包含数据库口令，请加入版本控制忽略清单。

# —— 真实 MySQL 连接（scripts/db-schema-check.py 使用）——
# 未配置时跳过真实库核对，SQL 照常输出并在文档“待确认与需求冲突”段注明。
# 优先 pymysql（uv run 自动隔离安装），无 pymysql 时降级本机 mysql 客户端。
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# —— Mermaid 真实渲染器（scripts/check-mermaid.py 使用，可选）——
# 未配置时自动降级为脚本内置结构化 lint，不真实渲染。
# MMDC_PATH 指向 mermaid-cli（mmdc）可执行文件或其命令名；CHROME_PATH 指向 Chrome/Chromium 可执行文件。
MMDC_PATH=
CHROME_PATH=

# —— 真实前后端项目路径（风格对齐用，可选）——
# 未配置时输出建议稿，风格遵循 skill 内置 references。
BACKEND_PATH=
FRONTEND_PATH=
